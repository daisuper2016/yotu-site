name: Update YouTube Data Cron Job

on:
  schedule:
    - cron: '0 * * * *'

jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call API to update data
        run: curl -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" "https://yotu-site.vercel.app/api/update"